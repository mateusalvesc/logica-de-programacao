let base, altura, area;

base = parseFloat(prompt("Digite a medida da base do retângulo em cm:"));
altura = parseFloat(prompt("Digite a medida da altura do retângulo em cm:"));

area = base * altura;

alert("A área deste retângulo é igual a: " + area + " cm");