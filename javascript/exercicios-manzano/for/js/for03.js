let somador = 0;

for (let contador = 1; contador < 101; contador++) {
    somador += contador;
    //console.log(somador);
}
alert("O total da soma dos cem primeiros números inteiros é igual a: " + somador);